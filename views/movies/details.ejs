<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('../partials/header') %>

    <h2><%= movie.Title %> (<%= movie.Year %>)</h2>
    <img
      src="<%= movie.Poster !== 'N/A' ? movie.Poster : '/icons/no-poster.jpg' %>"
      alt="poster"
      style="height: 300px"
    />

    <p><strong>Genre:</strong> <%= movie.Genre %></p>
    <p><strong>Plot:</strong> <%= movie.Plot %></p>
    <p><strong>Director:</strong> <%= movie.Director %></p>
    <p><strong>Actors:</strong> <%= movie.Actors %></p>
    <p><strong>IMDB Rating:</strong> <%= movie.imdbRating %></p>

    <a href="/movies">← Back to search</a>

    <h3>Reviews</h3>
    <% if (reviews.length === 0) { %>
    <p>No reviews yet. Be the first to review!</p>
    <% } else { %>
    <ul>
      <% reviews.forEach(r => { %>
      <li>
        <strong><%= r.username %></strong> rated <%= r.rating %>/5<br />
        <%= r.review_text %><br />
        <small><%= r.created_at.toLocaleString() %></small>
      </li>
      <% }) %>
    </ul>
    <% } %> <% if (currentUser) { %>
    <h3>Leave a Review</h3>
    <form action="/movies/<%= movie.imdbID %>/reviews" method="POST">
      <label>Rating (1-5):</label>
      <input type="number" name="rating" min="1" max="5" required /><br />

      <label>Comment:</label><br />
      <textarea name="comment" rows="3" cols="40" required></textarea><br />

      <button type="submit">Submit Review</button>
    </form>
    <% } else { %>
    <p><a href="/login">Login</a> to leave a review.</p>
    <% } %> <%- include('../partials/footer') %>
  </body>
</html>
